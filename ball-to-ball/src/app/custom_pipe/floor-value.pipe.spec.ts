import { FloorValuePipe } from './floor-value.pipe';

describe('FloorValuePipe', () => {
  it('create an instance', () => {
    const pipe = new FloorValuePipe();
    expect(pipe).toBeTruthy();
  });
});
